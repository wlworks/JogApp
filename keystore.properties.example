# 複製成 keystore.properties（已 gitignore）並填入 upload key 資訊。
# 沒有 keystore.properties 時 bundleRelease 仍可執行，只是產出未簽章的 bundle。
# storeFile 相對於 repo 根目錄。
storeFile=keystore/upload-keystore.jks
storePassword=
keyAlias=upload
keyPassword=
